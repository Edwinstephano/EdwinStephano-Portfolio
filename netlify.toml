[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/success.html"
  to = "/success.html"
  status = 200

# SPA fallback so React Router routes work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
